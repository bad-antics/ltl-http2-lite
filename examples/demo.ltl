// examples/demo.ltl — runnable demo for ltl_http2_lite

import std::io
use ltl_http2_lite as lib

fn main() {
    io::println("ltl_http2_lite demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}